apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  namespace: REPLACE_NAMESPACE
  name: agent-vs-REPLACE_POD_NAME
spec:
  gateways:
  - default/nextensio-ingressgateway
  hosts:
  - "REPLACE_GW"
  http:
  - match:
    - headers:
        x-nextensio-connect:
          prefix: REPLACE_POD_NAME
    route:
    - destination:
        host: REPLACE_POD_NAME-http-outside
        port:
          number: 443
